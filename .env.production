# The Turnstile site key for kitsorfan.com. Public by design: it is printed
# into the page for every visitor, and Turnstile only honours it on the
# hostnames the widget lists. The matching secret lives in the Worker alone,
# as the TURNSTILE_SECRET secret, and is never committed.
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAFEZckSBIBev7k6H
