# Turnstile's published always-passes test site key, for `npm run worker:dev`
# (which builds in the "worker" mode, so it reads this file rather than
# .env.development). The real site key goes in .env.production.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
